fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pypinyin>=0.53
pillow>=10.0
numpy>=1.26
rapidocr_onnxruntime>=1.3
# Optional but recommended: frequency-aware Chinese word segmentation.
# Without it, the app falls back to a simpler greedy dictionary matcher.
jieba>=0.42
